fastapi==0.135.2
uvicorn==0.38.0
python-multipart==0.0.22
pydantic==2.12.4

python-pptx==1.0.2
pytesseract==0.3.13
Pillow==11.3.0
pdf2image==1.17.0
pdfplumber==0.11.8

numpy==1.26.4
transformers==4.44.2

# Install PyTorch separately so its build matches the local CUDA environment.
# Do not install a CPU-only build for the GPU version of this project.
#
# FAISS is installed through conda-forge:
# conda install -c conda-forge faiss-cpu=1.9.0
